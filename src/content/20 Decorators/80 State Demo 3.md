```js
  //...
  render() {
    return (
      <div>
        <p>{this.count}</p>
        <button
          type="button"
          onClick={() => this.incrementCount()}
        >Plus 1</button>
      </div>
    );
  }
}
```
